apiVersion: v1
kind: Namespace
metadata: { name: demo }
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo
spec:
  selector: { app: demo-app }
  ports:
    - port: 80
      targetPort: 8000
      name: http
